package ${package.Param};

import io.swagger.annotations.ApiModel;
import lombok.Data;

/**
 * <p>
 * ${moduleTitle} ${table.comment!} 创建参数
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
@ApiModel("${moduleTitle}-${table.comment!}-创建参数")
@Data
public class ${tableName}CreateParam {



}
